# Production builds (e.g. Vercel) ship in self-contained DEMO MODE: the dashboard
# serves baked-in late-blight fixtures and needs no backend.
# To point a production build at a real backend instead, set VITE_DEMO_MODE=false
# and VITE_API_BASE_URL=https://your-api-host.
VITE_DEMO_MODE=true
